<%= form_for(@article) do |f|%>
  <div class="field">
    <%= f.text_area :title, placeholder:"タイトル" %>
  </div>  
  <div class="field">
    <%= f.text_area :content, placeholder:"記事内容" %>
  </div>
  <%= f.submit"Post", class: "btn btn-primary" %>
<% end %>